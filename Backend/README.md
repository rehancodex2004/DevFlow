# Backend

## Install

npm install

## Configure

Copy `.env.example` to `.env`.

## Create schema

npm run db:migrate

## Run

npm run dev

Server: http://localhost:5001

All database access uses `pg` with parameterized PostgreSQL queries.
